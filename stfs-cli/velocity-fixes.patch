diff --git a/XboxInternals/Stfs/StfsPackage.cpp b/XboxInternals/Stfs/StfsPackage.cpp
index 5031e15..28f2823 100644
--- a/XboxInternals/Stfs/StfsPackage.cpp
+++ b/XboxInternals/Stfs/StfsPackage.cpp
@@ -1234,7 +1234,7 @@ void StfsPackage::WriteFileListing(bool usePassed, vector<StfsFileEntry> *outFis
         // check to see if we need to go to the next block
         if (firstCheck)
             firstCheck = false;
-        else if ((i + 1) % 0x40 == 0)
+        else if (i % 0x40 == 0)
         {
             // check if we need to allocate a new block
             INT24 nextBlock;
@@ -1564,7 +1564,10 @@ void StfsPackage::FindDirectoryListing(vector<string> locationOfDirectory, StfsF
         StfsFileListing **out)
 {
     if(locationOfDirectory.size() == 0)
+    {
         *out = start;
+        return;
+    }
 
     bool finalLoop = (locationOfDirectory.size() == 1);
     for (DWORD i = 0; i < start->folderEntries.size(); i++)
@@ -1575,11 +1578,11 @@ void StfsPackage::FindDirectoryListing(vector<string> locationOfDirectory, StfsF
             if (finalLoop)
                 *out = &start->folderEntries.at(i);
             else
-                for (DWORD i = 0; i < start->folderEntries.size(); i++)
-                    if(*out == NULL)
-                        FindDirectoryListing(locationOfDirectory, &start->folderEntries.at(i), out);
-                    else
-                        break;
+                // CMZMod fix #4: recurse only into the matched child. Upstream recursed
+                // into EVERY folder at this level, so two same-named folders under
+                // different parents resolved to the wrong one ("Directory already
+                // exists" on CreateFolder, wrong-folder file injection).
+                FindDirectoryListing(locationOfDirectory, &start->folderEntries.at(i), out);
 
             break;
         }
